# GoDark SDK credentials
# Copy this file to .env and fill in your API credentials.

# --- Hosted / testnet (API key pair) ---
GODARK_API_KEY_ID=
GODARK_API_SECRET=
GODARK_PASSPHRASE=
# GODARK_EDGE_URL=wss://api.godark-dex.com

# --- Localnet (gdx-cli `gdx up`; legacy API key) ---
# GODARK_EDGE_URL=ws://127.0.0.1:13300
# GODARK_API_KEY=test-key-1
# GODARK_USER_UUID=00000000-0000-4000-8000-000000000001
# Required for WS Noise XK handshake — copy from sequencer log after `gdx up`:
#   rg -a 'noise_static_public=' <gdx-cli>/state/.logs/instance-*.log
# GDX_NOISE_STATIC_PUBLIC_KEY=
# Also required once: bind owner_pk for that UUID (see spline_order_example.rs header).

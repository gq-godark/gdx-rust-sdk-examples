# SDK reference (repo root)

Consumer-facing API documentation is maintained in [`bundle/SDK_REFERENCE.md`](bundle/SDK_REFERENCE.md)
(the same file is copied to the root of released ZIP bundles as `SDK_REFERENCE.md`).
